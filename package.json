{
  "name": "find-my-agent",
  "private": true,
  "version": "0.1.0",
  "description": "A spatial live-map (Find My-style) for parallel Claude Code agents.",
  "workspaces": [
    "server",
    "client",
    "hooks"
  ],
  "scripts": {
    "server": "bun run server/src/index.ts",
    "demo": "bun run demo/simulate.ts",
    "client": "bun run --cwd client dev",
    "hooks:install": "bun run hooks/install.ts",
    "dev": "echo 'Run in 3 terminals: `bun run server`, `bun run client`, then `bun run demo`'"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
